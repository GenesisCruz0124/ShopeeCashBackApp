# Leave empty for local dev (Vite proxies /api to the backend, see vite.config.ts).
# In production, set this to the deployed backend's public URL, e.g. https://backend.up.railway.app
VITE_API_BASE_URL=
